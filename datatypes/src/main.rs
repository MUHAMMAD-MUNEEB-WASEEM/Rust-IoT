fn main() {

    //These all are scalar data types
    let age = 20;//int
    let percentage = 80.3;//float
    let grade = 'A';//char
    let passed = true;//boolean

    let mut neg_number: i32 = -5;//defining signed type using i
    println!("{}", neg_number);
    //or 
    neg_number = -8;//without defining signed type
    println!("{}", neg_number);
    
}